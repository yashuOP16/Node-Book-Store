<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <title>Transaction History</title>
</head>
<body>
  <div class="container mt-5">
    <h1>Transaction History</h1>
    <a href="/newadd" class="btn btn-primary mb-3">Add Transaction</a>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Type</th>
          <th>Category</th>
          <th>Amount</th>
          <th>Description</th>
          <th>Date</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% transactions.forEach(transaction => { %>
          <tr>
            <td><%= transaction.type %></td>
            <td><%= transaction.category %></td>
            <td><%= transaction.amount %></td>
            <td><%= transaction.description %></td>
            <td><%= transaction.date ? transaction.date.toDateString() : 'No Date Provided' %></td>
            <td>
              <a href="/transactions/<%= transaction._id %>/edit" class="btn btn-info btn-sm">Edit</a>
              <form action="/transactions/<%= transaction._id %>/delete" method="POST" class="d-inline">
                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
